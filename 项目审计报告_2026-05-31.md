# 3D打印平台全面项目审计报告

> 生成日期：2026-05-31
> 项目：打个东西 - 3D打印智造中心
> 架构：前端 React 18 + TypeScript + Vite (localhost:3000) → API 后端 FastAPI (localhost:8001) → MySQL 数据库

---

## 目录

1. [系统架构总览](#一系统架构总览)
2. [前后端 API 对账](#二前后端-api-对账)
3. [数据库表结构](#三数据库表结构)
4. [各子系统功能评估](#四各子系统功能评估)
5. [真实数据 vs 模拟数据](#五真实数据-vs-模拟数据)
6. [用户体验缺失](#六用户体验缺失)
7. [安全性问题](#七安全性问题)
8. [并发与死锁风险](#八并发与死锁风险)
9. [架构设计问题](#九架构设计问题)
10. [已修复问题记录](#十已修复问题记录)
11. [修复优先级建议](#十一修复优先级建议)

---

## 一、系统架构总览

```
┌─────────────────────────────────────────────────────┐
│                  前端 Vite                           │
│  ┌────────────────────────────────────┐             │
│  │  React 18 + TypeScript + Tailwind  │             │
│  │  ┌─────┐ ┌───────┐ ┌────────────┐  │             │
│  │  │18个 │ │9个组件│ │15个API层文件│  │             │
│  │  │页面  │ │       │ │            │  │             │
│  │  └─────┘ └───────┘ └────────────┘  │             │
│  │                                     │             │
│  │  Vite Proxy:                        │             │
│  │    /api     → localhost:8001        │             │
│  │    /uploads → localhost:8001        │             │
│  └────────────────────────────────────┘             │
│                          │                          │
│  另：E:\3d\admin 独立Vite项目 (localhost:3002)       │
│      管理后台，亮色主题，独立 Token (3dprint_admin_token)│
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│               后端 FastAPI (8001)                     │
│  ┌────────────────────────────────────────────────┐  │
│  │  接口层: 15个路由模块 (auth, models, orders,    │  │
│  │          cart, addresses, wallet, discussions,  │  │
│  │          model_comments, announcements,         │  │
│  │          notifications, reports, materials,     │  │
│  │          help, favorites)                       │  │
│  ├────────────────────────────────────────────────┤  │
│  │  业务层: 报价计算, 支付处理, 切片引擎            │  │
│  ├────────────────────────────────────────────────┤  │
│  │  数据层: SQLAlchemy ORM → MySQL                 │  │
│  └────────────────────────────────────────────────┘  │
│                          │                          │
│  BambuStudio CLI 切片引擎 (E:\tuoz\...)              │
│  ↓ 3MF → STL → 切片 → result.json                   │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│               MySQL 数据库 (3dprint)                  │
│  21张表: user, model3d, order, cart_item, address,  │
│          discussion, discussion_reply, model_comment │
│          announcement, wallet_transaction, report    │
│          notification, follow, activity, conversation│
│          message, user_conversation_status, material │
│          help_article, favorite                      │
└─────────────────────────────────────────────────────┘
```

---

## 二、前后端 API 对账

### ✅ 已匹配（前端API调用 → 后端路由正常）

| 前端 API 文件 | exports | 后端 route module | 状态 |
|---|---|---|---|
| `api/auth.ts` | 10 函数 | `auth.py` | ✅ |
| `api/models.ts` | 8 函数 | `models.py` | ✅ |
| `api/orders.ts` | 11 函数 | `orders.py` | ✅ |
| `api/cart.ts` | 5 函数 | `cart.py` | ✅ |
| `api/addresses.ts` | 5 函数 | `addresses.py` | ✅ |
| `api/wallet.ts` | 3 函数 | `wallet.py` | ✅ |
| `api/discussions.ts` | 8 函数 | `discussions.py` | ✅ |
| `api/favorites.ts` | 3 函数 | `favorites.py` | ✅ |
| `api/modelComments.ts` | 6 函数 | `model_comments.py` | ✅ |
| `api/announcements.ts` | 4 函数 | `announcements.py` | ✅ |
| `api/notifications.ts` | 17 函数 | `notifications.py` | ✅ |
| `api/help.ts` | 3 函数 | `help.py` | ✅ |
| `api/materials.ts` | 3 函数 | `materials.py` | ✅ |
| `api/reports.ts` | 2 函数 | `reports.py` | ✅ |

### ⚠️ 硬编码问题（3处）

| 文件 | 行 | 内容 | 影响 |
|------|----|------|------|
| `api/discussions.ts` | L3 | `const API = 'http://localhost:8000'` | 废弃变量（实际用相对路径 `/api/...`），混淆 |
| `pages/Community.tsx` | L14 | `const API_BASE = 'http://localhost:8000'` | 同上 |
| `pages/AdminLogin.tsx` | L133 | `href="http://localhost:3000"` | 硬编码前台地址，部署需修改 |

> 实际 API 调用通过 Vite proxy：`/api/*` → `localhost:8001`（后端真实端口）。硬编码变量虽未使用，但误导维护者。

---

## 三、数据库表结构

> 共 21 张表，具体字段略。核心表关系如下：

```
User ──┬── Model3D (uploader_id)
       ├── Order (user_id)
       ├── Address (user_id)
       ├── Discussion (user_id)
       ├── ModelComment (user_id)
       ├── WalletTransaction (user_id)
       ├── Notification (user_id)
       ├── Follow (follower_id / following_id)
       ├── Activity (user_id)
       ├── Favorite (user_id)
       ├── Report (reporter_id)
       ├── Conversation (参与方)
       └── Message (sender_id)

Model3D ──┬── CartItem (model_id)
          ├── Order (model_id)
          ├── ModelComment (model_id)
          ├── Favorite (model_id)
```

---

## 四、各子系统功能评估

### 4.1 用户认证 🔒

| 功能 | 状态 | 说明 |
|------|------|------|
| 手机号+密码注册/登录 | ✅ | |
| 验证码发送 | ✅ | 阿里云短信已配置，mock=False |
| 演示手机号硬编码 | ⚠️ | `LoginPage.tsx:218,224` 展示13800002222/13800001111 |
| Token持久化 | ✅ | `3dprint_token` + `3dprint_user` |
| 头像上传 | ✅ | 支持 jpg/png/gif/webp |
| 角色管理 | ✅ | USER/ADMIN |
| 用户资料编辑 | ✅ | 昵称/手机号改密 |
| 忘记密码 | ❌ | 无此功能 |

### 4.2 模型管理 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 3MF上传 | ✅ | 格式限 .3mf |
| BambuStudio精确切片 | ✅ | 重量/体积/尺寸 |
| GLB转换+3D预览 | ✅ | model-viewer |
| 分类/搜索/排序 | ✅ | 服务端分页 |
| 缩放打印 (25%~300%) | ✅ | 价格×scale³动态计算 |
| 自动定价 | ✅ | 重量×材料单价 |
| 管理员审核 | ✅ | 通过→发布+奖励Nova豆 |
| 重新切片 | ✅ | 管理功能 |
| **模型详情无需登录** | ❌ | 需登录才能查看 |

### 4.3 购物车+订单

| 功能 | 状态 |
|------|------|
| 购物车CRUD | ✅ |
| 下单+结算（材料/层高/填充选择） | ✅ |
| 订单状态流转（待付款→已支付→已发货→已完成） | ✅ |
| 余额/Nova豆支付 | ✅ |
| 微信/支付宝模拟支付 | ✅ (模拟截图) |
| 取消订单退款（余额） | ✅ |
| 订单删除（已完成/已取消） | ✅ |
| **并发扣余额安全** | ❌ 无锁 |
| **支付取消事务一致性** | ❌ |

### 4.4 钱包+Nova豆

| 功能 | 状态 | 说明 |
|------|------|------|
| 余额查询 | ✅ | |
| 充值 | ⚠️ | 模拟，无真实入金通道 |
| 审核奖励Nova豆 | ✅ | 管理后台 |
| Nova豆折扣（每豆0.5%，上限50%） | ✅ | |
| 订单扣款+退款 | ✅ | 退余额 |

### 4.5 社区系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 话题CRUD | ✅ | |
| 嵌套评论（parent_id递归） | ✅ | |
| 4种分类（综合/求助/作品展示/技术交流）| ✅ | |
| 管理员置顶+锁定 | ✅ | |
| 搜索+分页 | ✅ | |

### 4.6 社交系统

| 功能 | 状态 |
|------|------|
| 关注/取关 | ✅ |
| 用户动态时间线 | ✅ |
| 通知列表+红点计数 | ✅ |
| 私信三栏布局 | ✅ |
| 未读私信计数 | ✅ |

### 4.7 收藏/评论

| 功能 | 状态 |
|------|------|
| 模型收藏+取消 | ✅ |
| 模型评论+回复 | ✅ |
| 收藏列表（用户中心） | ✅ |
| 收藏检查 | ✅ |

### 4.8 管理后台

> 独立 Vite 项目 `E:\3d\admin`，亮色主题

| 功能 | 状态 |
|------|------|
| 订单管理（搜索/状态/发货） | ✅ |
| 模型管理（审核/删除/重新切片） | ✅ |
| 用户管理（设/撤管理员） | ✅ |
| 评论/帖子/举报管理 | ✅ |
| 公告管理 | ✅ |
| 材料/帮助管理 | ✅ |
| 独立 Token | ⚠️ 使用 `3dprint_admin_token`，与前台不共享登录态 |

---

## 五、真实数据 vs 模拟数据

### ✅ 真实数据

| 项目 | 说明 |
|------|------|
| MySQL 数据库 | 所有业务数据持久化 |
| BambuStudio CLI 切片 | 真实切片引擎计算重量/体积/尺寸 |
| 模型文件物理存储 | uploads/models, uploads/glb, uploads/thumbs |
| 头像文件物理存储 | uploads/avatars |
| 阿里云短信 | 已配真实 AccessKey，mock=False |

### ⚠️ 模拟/占位数据

| 项目 | 说明 | 状态 |
|------|------|------|
| 微信支付 | 前端弹出支付二维码模拟截图 | ⚠️ 未对接真实微信 V3 API |
| 支付宝支付 | 同上 | ⚠️ |
| 钱包充值 | 直接改余额，无真实入金通道 | ⚠️ |
| 订单退款 | 退余额（非微信原路） | ⚠️ |
| 物流轨迹 | 快递100免费接口（已写代码但未验证） | ⚠️ 未测试 |
| 演示账号/密码 | 硬编码于 LoginPage | ⚠️ |
| 管理员密码 `admin123` | 代码 seed 中明文 | ⚠️ |

---

## 六、用户体验缺失

### 🚨 最严重

| # | 缺失功能 | 位置 | 影响 |
|---|---------|------|------|
| **1** | **模型详情页需登录才能看** | `App.tsx` ModelDetail 路由被 `ProtectedRoute` 包裹 | 新用户点击首页模型→跳登录页，转化率暴跌 |
| **2** | **无"忘记密码"流程** | `LoginPage.tsx` | 用户忘了密码就永远登不上了 |
| **3** | **无搜索自动补全/联想** | `Home.tsx`, `Community.tsx` | 搜索体验粗糙 |
| **4** | **购物车无实时价格计算** | `CartDrawer.tsx` | 改数量/材料后不会自动重算总价 |
| **5** | **通知无"全部已读"按钮** | `UserCenter.tsx` | 红点积累后要逐个点击 |
| **6** | **无加载骨架屏** | 全部页面 | 只显示 "Loading..." 文字 |
| **7** | **无错误边界（Error Boundary）** | 全局 | catch 到错误显示 `[object Object]` |
| **8** | **支付弹窗无倒计时/超时策略** | `OrderPage.tsx` | 用户不知道支付是否过期 |
| **9** | **首页无"热门/推荐"排序** | `Home.tsx` | 新用户看不到最受欢迎的模型 |
| **10** | **用户中心过于臃肿** | 1521 行文件 | 所有 Tab 挤在一起，体验/性能都差 |

### 🟢 次要

| # | 缺失 | 说明 |
|---|------|------|
| 11 | 无购物车数量角标 | Navbar 购物车图标无数字 |
| 12 | 模型详情无相似推荐 | 看了 bunny，下面没有类似的 |
| 13 | 评论无排序（最新/最热） | 只有按时间 |
| 14 | 无面包屑导航 | 用户不知道自己在哪 |
| 15 | 管理后台无操作确认 | 删除/改角色前没弹窗确认 |

---

## 七、安全性问题

### 🔴 高危

| # | 问题 | 位置 | 风险说明 |
|---|------|------|----------|
| 1 | **SECRET_KEY 硬编码弱密钥** | `config.py:27` | 暴露代码即可伪造任意 JWT token |
| 2 | **MySQL密码硬编码 root/root** | `config.py:20` | 代码泄露=数据库沦陷 |
| 3 | **无速率限制** | 全部 API | 暴力登录、短信轰炸、爬虫 |
| 4 | **验证码内存存储** | `auth.py` dict | 多 worker 不共享，用户手机收验证码到另一个进程验证失败 |
| 5 | **余额扣减无原子操作** | `orders.py:pay_order` | 并发下单余额变负数 |
| 6 | **取消订单和支付可同时成功** | 同一订单既付又退 | 资金不一致 |

### 🟡 中危

| # | 问题 | 位置 |
|---|------|------|
| 7 | JWT 7天过期，无 refresh token | `config.py:29` |
| 8 | 无 CSRF 防护 | 全部 POST |
| 9 | 文件上传仅检查扩展名 | `models.py`, `auth.py:avatar` |
| 10 | 演示码 `123456` 通用 | `auth.py:DEMO_CODE` |
| 11 | CORS 不包含生产域名 | `config.py` |
| 12 | 错误信息泄露 | `raise HTTPException(500, detail=str(e))` |
| 13 | token 验证无黑名单 | 注销后 token 仍有效 |

### 🟢 低危

| # | 问题 |
|---|------|
| 14 | 无 HTTPS 强制跳转 |
| 15 | 无请求审计日志 |
| 16 | 无监控/告警 |

---

## 八、并发与死锁风险

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| 🔴 1 | **订单支付+取消无数据库行锁** | `orders.py` | 两个请求同时处理同一订单→状态紊乱 |
| 🔴 2 | **余额扣减非原子** | `orders.py:pay_order` | `user.balance -= x` 读→改→写三步，并发覆盖 |
| 🔴 3 | **下单+支付非事务** | `create_order` 创建后调 `process_order_payment` | 支付中途失败，订单已创建未支付 |
| 🟡 4 | **删除模型手动清关联，无事务包裹** | `models.py:delete_model` | 步骤多，部分成功部分失败 |
| 🟡 5 | **`db.rollback()` 使用混乱** | 多处 | 回滚后 session 不干净，继续使用报错 |
| 🟡 6 | **连接池无限制** | `database.py` | 默认 `pool_size=5, max_overflow=10`，并发高时耗尽 |
| 🟡 7 | **`db.flush()` 部分提交** | 多处 | = 事务部分写入，后续失败不能完整回滚 |

---

## 九、架构设计问题

| 问题 | 详情 |
|------|------|
| **前端文件体积巨大** | `UserCenter.tsx` 1521行、`AdminDashboard.tsx` 1120行、`ModelDetail.tsx` 929行、`OrderPage.tsx` 691行、`DiscussionDetail.tsx` 617行 |
| **无状态管理** | 未用 Redux/Zustand，页面间数据无法共享 |
| **无错误边界** | catch 到错误直接 `[object Object]` |
| **无日志中间件** | 请求无统一日志 |
| **异常处理不一致** | 有的用 `print()`，有的用 `logging` |
| **无 Celery/异步任务** | BambuStudio 切片是同步的，阻塞请求直到切片完成（2-4秒） |

---

## 十、已修复问题记录

### 2026-05-31 修复

| # | 问题 | 修复内容 |
|---|------|----------|
| 1 | `bambu_slicer.slice_model()` 返回缺 objects 字段 | 给返回字典加入完整 objects bbox 数据 |
| 2 | `ModelUpdate` schema 缺重量/体积/尺寸字段 | 添加 `weight/volume/dimensions_x/y/z` |
| 3 | 已有模型 ID 43~46 参数全为 NULL | 手动切片获得正确值，API 回填 |
| 4 | 前端提示文字错误 | "支持STL/OBJ/3MF/STEP/STP" → "仅支持 3MF 格式" |
| 5 | 临时调试脚本 | 清理 E:\3d\ 根目录 4 个调试脚本 |

### 各模型回填数据

| ID | 名称 | 重量(g) | 体积(cm³) | 尺寸(mm) | 价格(元) |
|----|------|---------|-----------|----------|---------|
| 43 | 3号 | 2.35 | 2.05 | 32.0×22.3×19.7 | 1.41 |
| 44 | 中指幽灵 | 46.63 | 48.04 | 66.0×75.7×85.6 | 27.98 |
| 45 | 卷毛小狗摆件 | 40.89 | 193.48 | 65.1×64.4×90.0 | 24.53 |
| 46 | V1.1(舱门开启) | 94.27 | 3.94 | 60.2×50.8×120.9 | 56.56 |

---

## 十一、修复优先级建议

### 第一轮：用户体验 Quick Wins（半天）

| # | 任务 | 修改 | 难度 |
|---|------|------|------|
| 1 | ModelDetail 取消登录保护 | `App.tsx` ProtectedRoute 移除 | ⭐ |
| 2 | 忘记密码流程 | 后端+前端 手机验证码重置密码 | ⭐⭐ |
| 3 | 购物车实时价格计算 | `CartDrawer.tsx` 监听变化重算 | ⭐ |
| 4 | 通知"全部已读"按钮 | 前端 + 后端批量已读接口 | ⭐ |
| 5 | 删除 LoginPage 演示手机号 | 移除硬编码 | ⭐ |

### 第二轮：安全加固（1天）

| # | 任务 | 难度 |
|---|------|------|
| 1 | SECRET_KEY → 环境变量 (32位随机) | ⭐ |
| 2 | MySQL密码 → 环境变量 | ⭐ |
| 3 | 速率限制 (`slowapi`) | ⭐⭐ |
| 4 | 文件上传 MIME 检测 | ⭐ |
| 5 | 验证码 → Redis | ⭐⭐⭐ |

### 第三轮：并发安全（1天）

| # | 任务 | 难度 |
|---|------|------|
| 1 | 余额扣减改为原子 SQL (`UPDATE SET balance=balance-x WHERE balance>=x`) | ⭐⭐ |
| 2 | 订单支付+取消加行锁 (`SELECT ... FOR UPDATE`) | ⭐⭐ |
| 3 | 下单+支付包裹一个事务 | ⭐⭐ |
| 4 | 删除模型关联清理事务包裹 | ⭐ |

### 第四轮：架构优化（2-3天）

| # | 任务 | 难度 |
|---|------|------|
| 1 | 超大组件拆分（UserCenter/AdminDashboard） | ⭐⭐⭐ |
| 2 | 引入状态管理 (Zustand) | ⭐⭐ |
| 3 | 加载骨架屏 + 错误边界 | ⭐⭐ |
| 4 | 异步切片（Celery / 后台任务） | ⭐⭐⭐ |
| 5 | Git 初始化 + CI/CD | ⭐⭐ |

---

## 总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 核心功能 | 75/100 | 该有的都有 |
| 用户体验 | 40/100 | 登录屏障 + 很多交互细节缺失 |
| 安全性 | 30/100 | 密钥/密码硬编码 + 无速率限制 |
| 并发 | 20/100 | 无锁 + 余额不是原子操作 |
| 架构 | 50/100 | 文件过大 + 后端耦合 |

> **一句话总结**：核心功能完整可用，但离生产级还有明显的安全/并发/UX差距。





状态机跳转错误、边界值漏洞。外部调用未处理超时/异常。缺少唯一索引、参数未校验。回调未验签、重试未幂等。关键操作无日志、无告警。错误提示暴露堆栈、重复提交无反馈。无回滚方案、限流未配置，这些工程问题我们预案或者考虑到吗